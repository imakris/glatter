glatter

An OpenGL loader library, with support for GL, GLES, EGL, GLX, WGL.

The project is currently in an early stage of development.
It has only been tested on Windows, and needs to be ported to other systems.
